import { BrexTransactions } from "./index";

describe("brex-transactions-typescript-sdk", () => {
    it("initialize client", async () => {
        const brextransactions = new BrexTransactions({
        });
    });
});
